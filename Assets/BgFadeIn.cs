using UnityEngine;
using System.Collections;

public class BgFadeIn : MonoBehaviour {

	/*public float timer = 5.0f;
	public GameObject background;

	void Start() 
	{
		this.renderer.material.color.a = 1.0f;
	}
	
	void Update()
	{
		
		timer = timer - Time.deltaTime;
		
		if(timer >= 0)
		{
			background.color.a -= 0.2 * Time.deltaTime;
		}
		
		if(timer <= 0)
		{
			timer = 0;
			Destroy(gameObject);
		}
		
		
	}*/
}
